<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/relative_addnew"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/screen_background"
    android:orientation="vertical"
    >
    <include
        android:id="@+id/my_toolbar"
        layout="@layout/mytoolbar"/>

    <LinearLayout
        android:layout_below="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/screen_background"
        android:orientation="vertical"
        >
        <android.support.design.widget.TabLayout
            android:id="@+id/jobs_tab_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/screen_background"
            app:tabTextColor="#FFFFFF"
            app:tabSelectedTextColor="#FFFFFF"
            app:tabIndicatorColor="@color/colorPrimaryDark"
            app:tabIndicatorHeight="0dp"
            app:tabBackground="@drawable/ic_attach"
            app:tabPaddingStart="0.25dp"
            app:tabPaddingEnd="0.25dp"
            />
        <android.support.v4.view.ViewPager
            android:id="@+id/jobs_pager"
            android:layout_width="match_parent"
            android:layout_height="fill_parent"
            android:layout_below="@id/jobs_tab_layout"
            />
    </LinearLayout>
</RelativeLayout>
